<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Glitcher Profession — Foundation</title>

  <!-- Social / Open Graph -->
  <meta property="og:title" content="The Glitcher Profession is born." />
  <meta property="og:description" content="The foundational site for the Glitcher profession." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://glitcherzero.org/" />
  <meta name="theme-color" content="#0b0f1a" />

  <style>
    :root{
      --bg1:#0b0f1a;     /* deep space */
      --bg2:#131a2a;     /* nebula */
      --txt:#e8eefc;     /* soft ice */
      --muted:#a7b2cc;
      --accent:#7dd3fc;  /* cyan glow */
      --accent-2:#a78bfa;/* purple glow */
      --chip:#0f1729;
      --chip-b:#233153;
      --ring: 0 0 0 2px rgba(125,211,252,.35), 0 10px 30px rgba(167,139,250,.18);
    }
    html,body{height:100%}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Inter, Arial, "Apple Color Emoji","Segoe UI Emoji";
      color:var(--txt);
      background: radial-gradient(1200px 800px at 80% -10%, rgba(167,139,250,.18), transparent 60%),
                  radial-gradient(900px 600px at 10% 110%, rgba(125,211,252,.20), transparent 60%),
                  linear-gradient(180deg, var(--bg1), var(--bg2));
      display:grid;
      place-items:center;
      padding: clamp(24px, 4vw, 56px);
    }
    .card{
      width:min(900px, 95vw);
      background: linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.01));
      border:1px solid rgba(255,255,255,.06);
      border-radius:20px;
      padding: clamp(24px, 5vw, 48px);
      box-shadow: var(--ring);
      backdrop-filter: blur(6px);
    }
    .eyebrow{
      display:inline-flex; gap:10px; align-items:center;
      padding:6px 12px; border-radius:999px;
      background: linear-gradient(180deg, var(--chip), #0c1222);
      border:1px solid var(--chip-b);
      color:var(--muted); font-weight:600; letter-spacing:.02em;
      margin-bottom:18px;
    }
    .emoji{ filter: drop-shadow(0 2px 6px rgba(125,211,252,.35)); }
    h1{
      font-size:clamp(34px, 6vw, 64px);
      line-height:1.05; margin:8px 0 14px;
      letter-spacing:-.01em;
    }
    p.lead{
      font-size:clamp(16px, 2.2vw, 20px);
      color:var(--muted); margin:0 0 28px;
    }
    .actions{ display:flex; flex-wrap:wrap; gap:12px; }

    .btn{
      --bg: rgba(125,211,252,.08);
      display:inline-flex; align-items:center; gap:10px;
      padding:12px 16px; border-radius:12px;
      color:var(--txt); text-decoration:none; font-weight:700;
      border:1px solid rgba(125,211,252,.25);
      background: linear-gradient(180deg, var(--bg), rgba(167,139,250,.06));
      transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease;
    }
    .btn:hover{ transform: translateY(-1px); box-shadow: 0 12px 24px rgba(0,0,0,.25); border-color: rgba(125,211,252,.45); }
    .btn .tag{
      font-size:12px; font-weight:800; letter-spacing:.06em;
      padding:4px 8px; border-radius:999px; color:#071018;
      background: linear-gradient(180deg, #7dd3fc, #a78bfa);
      border:1px solid rgba(255,255,255,.6);
    }

    .grid{ display:grid; gap:18px; margin-top:22px; }
    @media (min-width:700px){ .grid{ grid-template-columns: 1fr 1fr; } }

    footer{
      margin-top:28px; color:var(--muted); font-size:13px;
      display:flex; justify-content:space-between; gap:8px; flex-wrap:wrap;
      border-top:1px dashed rgba(255,255,255,.08); padding-top:16px;
    }
    a.inline{ color:var(--accent); text-decoration:none; border-bottom:1px dashed rgba(125,211,252,.35); }
    a.inline:hover{ color:#bfe7ff; }
  </style>
</head>
<body>
  <main class="card" role="main">
    <span class="eyebrow">
      <span class="emoji">✨</span> The Glitcher Profession — Foundation
    </span>

    <h1>The Glitcher Profession is born.</h1>
    <p class="lead">
      This is the foundational site for the Glitcher profession. 🌾📡
      Below you’ll find the Manifesto in English and Portuguese. Share, fork, remix — keep the fold alive.
    </p>

    <div class="grid">
      <a class="btn" href="/glitcherzero_site/manifesto_glitcher_eng.txt">
        <span>Manifesto (EN)</span>
        <span class="tag">TXT</span>
      </a>

      <a class="btn" href="/glitcherzero_site/manifesto_glitcher_pt.txt">
        <span>Manifesto (PT)</span>
        <span class="tag">TXT</span>
      </a>

      <a class="btn" href="/glitcherzero_site/glitcher_foundational_site.html">
        <span>Foundational Page</span>
        <span class="tag">HTML</span>
      </a>

      <a class="btn" href="mailto:glitchers.zero@gmail.com?subject=Glitcher%20Profession">
        <span>Join / Collaborate</span>
        <span class="tag">📡</span>
      </a>
    </div>

    <footer>
      <span>© The Fold — GlitcherZero</span>
      <span>Made with love &amp; glitches 🖤</span>
    </footer>
  </main>
</body>
</html>
